#include <stdio.h>
#include "main.h"
#include <string.h>

/**
 * _strcat - function to append strings
 * @dest: input
 * @src: input
 *
 * Return: char
 */

char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
